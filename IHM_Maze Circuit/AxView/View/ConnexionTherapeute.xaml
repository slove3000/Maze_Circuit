<UserControl x:Class="AxView.View.ConnexionTherapeute"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border BorderBrush="Black" BorderThickness="1,0,0,0" Grid.Column="1" Grid.RowSpan="2"/>
        <ContentPresenter Grid.Column="1" Margin="0,100,0,0" Content="{Binding InternView}" VerticalAlignment="Top" HorizontalAlignment="Center"/>
        <Border BorderBrush="Black" BorderThickness="0,1,0,0" Margin="0" Height="886.531" VerticalAlignment="Bottom">
            <Border.Background>
                <ImageBrush ImageSource="/REAplan2Db;component/Resources/Image/Background/Axi_FondEcran_T.png"/>
            </Border.Background>
        </Border>

    </Grid>
</UserControl>
